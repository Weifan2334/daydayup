# 更新日志 (Changelog)

本文件记录明管家 / daydayup 的每个版本更新。格式参考 [Keep a Changelog](https://keepachangelog.com/)。

## [v0.3.4] - 2026-07-20

### ✨ 新增
- **日历「回顾」**：过去日期日视图展示「真实记录 / 任务完成 / 金币分布」三栏概览，并提供「DeepSeek 日报」入口（未配置 key 时优雅提示，不崩溃）。
- **设置页「备份与同步」**：说明数据同时备份在本地与腾讯云服务器，异地登录或软件更新时可自动找回任务、记录、目标等信息。
- **计划视图「未来计划规划」对话框**：输入模糊计划，由 DeepSeek 拆解为 4~8 条可执行事件安排（可一键采纳，带时间段）。
- **谋士「小锦囊」**：Q 版可爱动漫谋士形象，AI 推荐事件显示 `08:00–09:30` 时间段。
- **24 枚光阴金币「太极金币」库房**：亮金实体圆币 + 中央太极浮雕 + 边缘齿纹，随整点平滑暗淡。
- **今日视图双栏**：「作息时间轴（推荐）」与「每日真实记录（时间网格）」并排；真实记录网格支持拖拽移动 / 拉伸时长 / 点击空白新建。
- **AI 定制作息轴**：输入自身情况（工作/休息、居住地、天气、计划）生成覆盖式推荐作息，可「恢复默认」。
- **桌面端强化**：全局快捷键 `Ctrl+Shift+D` 唤起/隐藏窗口；拖拽 `.db` 备份导入；后端异常自动重启（最多 3 次退避）。

### 🐛 修复
- 修复 `/api/actual_records` 在高并发下偶发 **HTTP 500**（SQLite 单全局连接 → 改为每线程独立连接 + `busy_timeout`），过去日真实记录概览不再偶发空白。
- 修复 Electron 中 AI 作息 textarea 与真实记录 textarea **无法输入**（父级拖拽事件捕获冲突）。
- 修复 DeepSeek 连接失败（`services/ai.py` 缺失 `import json` 导致解析失败回退本地推演）。
- 修复模态框安装即弹出（`pwd-modal` 的 `display:flex` 覆盖 HTML `hidden` 属性）。

### 🔧 变更
- 主题色从竹青绿改回暖陶土金系（`--accent: #a05a2c` / `--accent-soft: #d4a373`）。
- 导航「目标」→「计划」并前移至「今日」之前；顺序为 计划 / 今日 / 回顾 / 日历 / 设置。
- 桌面端图标改为极简主题（墨青底 + 居中长剑 + 右上弯月）。

### 🔒 安全
- DeepSeek key 改为 **AES-256-GCM 密文**存储于 `deepseek.key`（打包进 asar），运行时由主进程解密注入后端内存，前端 / 磁盘 / asar 均无明文；已移除前端「AI 设置」面板与后端保存路由。

---

## 历史版本
- **v0.3.3** – NSIS 安装包（Git LFS）、模态框默认隐藏修复。
- **v0.3.2** – 武侠少侠主题桌面 / 托盘 / 任务栏图标。
- **v0.3.1 / v0.3.0** – 早期安装包版本。
